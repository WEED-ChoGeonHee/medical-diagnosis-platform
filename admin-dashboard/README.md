# 🩺 Admin Dashboard - SkinIQ Doctor 의사용 웹앱

의사가 환자 진단을 검토하고 최종 소견을 작성하는 React 웹 애플리케이션입니다.

## 기술 스택

- **프레임워크**: React 18
- **라우팅**: React Router v6
- **HTTP**: Axios
- **스타일**: CSS (커스텀)

---

## 폴더 구조

```
admin-dashboard/
├── public/
│   └── index.html           # HTML 템플릿
│
├── src/
│   ├── index.js             # React 진입점
│   ├── App.js               # 라우팅 설정 + 인증 상태 관리
│   ├── api.js               # API 호출 함수 (axios)
│   ├── index.css            # 전역 스타일
│   │
│   └── components/
│       ├── Login.js         # 🔐 의사 로그인
│       ├── Header.js        # 상단 네비게이션
│       ├── Dashboard.js     # 📊 통계 대시보드 (KPI)
│       ├── DiagnosisList.js # 📋 진단 목록 (필터, 페이지)
│       ├── DiagnosisDetail.js # ✍️ 진단 상세 + 소견 작성
│       ├── PatientList.js   # 👥 환자 목록
│       └── *.css            # 컴포넌트별 스타일
│
├── build/                   # 빌드 결과물 (배포용)
└── package.json
```

---

## 주요 기능

### 1. 의사 로그인
- 의사 역할(`role: doctor`) 계정만 접근 가능
- JWT 토큰 인증

### 2. 대시보드 (Dashboard.js)
- 총 진단 수, 대기중, 검토중, 완료 통계
- 최근 진단 목록

### 3. 진단 목록 (DiagnosisList.js)
- 전체 환자 진단 조회
- 상태별 필터링
- 페이지네이션

### 4. 진단 상세 (DiagnosisDetail.js) ⭐ 핵심
- 환자 정보 확인
- 업로드된 피부 사진 확인
- AI 진단 결과 검토
- **의사 소견 작성 및 저장**
- 상태 변경 (대기중 → 검토중 → 완료)

### 5. 환자 목록 (PatientList.js)
- 등록된 환자 목록 조회

---

## 로컬 실행

```bash
npm install
set PORT=3001 && npm start   # Windows
# PORT=3001 npm start        # Mac/Linux
```

`http://localhost:3001`에서 실행됩니다.

> ⚠️ 백엔드 서버(`localhost:5000`)가 먼저 실행되어야 합니다.

---

## 환경 설정

### 개발 환경 (package.json)
```json
{
  "proxy": "http://localhost:5000"
}
```

### 프로덕션 환경
빌드된 파일이 백엔드 서버의 `/admin` 경로에서 서빙됩니다.

---

## 빌드

```bash
npm run build
```

---

## 컴포넌트 흐름

```
App.js (라우팅)
│
├── /login → Login.js (의사 전용)
│
└── (인증 후)
    ├── /dashboard → Dashboard.js → 통계
    ├── /diagnoses → DiagnosisList.js → 진단 목록
    ├── /diagnoses/:id → DiagnosisDetail.js → 소견 작성
    └── /patients → PatientList.js → 환자 목록
```

---

## API 사용

모든 API는 `/api/admin/*` 엔드포인트를 사용합니다.

```javascript
// api.js 예시
export const getStats = () => api.get('/api/admin/stats');
export const getDiagnoses = (params) => api.get('/api/admin/diagnoses', { params });
export const updateDiagnosis = (id, data) => api.put(`/api/admin/diagnoses/${id}`, data);
```

---

## 스타일 가이드

- 배경색: `#0a0e27` (다크 테마)
- 메인 컬러: `#a855f7` (퍼플), `#00d4ff` (시안)
- 아이콘: 🔬 (의사용 구분)
- 폰트: Inter
